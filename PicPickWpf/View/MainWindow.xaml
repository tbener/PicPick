<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:commands="clr-namespace:PicPick.Commands"
        xmlns:uc="clr-namespace:PicPick.UserControls.View"
                      xmlns:controlzex="urn:controlzex" x:Class="PicPick.MainWindow"
                      mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="600" Width="800">

    <Window.InputBindings>
        <KeyBinding Command="{Binding OpenFileCommand}" Key="O" Modifiers="Ctrl"/>
        <KeyBinding Command="{Binding SaveCommand}" Key="S" Modifiers="Ctrl"/>
        <KeyBinding Command="{Binding AddActivityCommand}" Key="N" Modifiers="Ctrl"/>
    </Window.InputBindings>

    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open..." Command="{Binding OpenFileCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save" Command="{Binding SaveCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _As..." Command="{Binding SaveCommand}" CommandParameter="true" />
            </MenuItem>
            <MenuItem Header="Activity">
                <MenuItem Header="_Add..." Command="{Binding AddActivityCommand}" InputGestureText="Ctrl+N"/>
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock Text="Activities" DockPanel.Dock="Top" Margin="0,5"/>
                <ListBox x:Name="lstActivities" ItemsSource="{Binding CurrentProject.ActivityList}" SelectedItem="{Binding CurrentActivity}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </DockPanel>

            <Grid x:Name="Content" Grid.Column="1" DataContext="{Binding ActivityViewModel}">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="title" Height="Auto"/>
                    <RowDefinition x:Name="source" Height="Auto"/>
                    <RowDefinition x:Name="destinations" Height="*"/>
                    <RowDefinition x:Name="more_properties" Height="Auto"/>
                    <RowDefinition x:Name="buttons" Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" Text="{Binding Activity.Name, UpdateSourceTrigger=PropertyChanged, FallbackValue=Activity Name}" FontSize="16" FontWeight="Bold" Margin="0, 0, 0, 10"/>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <GridSplitter Grid.Column="0" HorizontalAlignment="Right" Width="3" Grid.RowSpan="2"/>

                    <DockPanel Grid.Row="0" Margin="0,0,5,0" LastChildFill="False">
                        <TextBlock Text="Source" DockPanel.Dock="Top" Height="23"/>
                        <uc:PathBrowserView DockPanel.Dock="Top" DataContext="{Binding SourceViewModel}"/>
                        <CheckBox DockPanel.Dock="Top" IsChecked="{Binding Activity.Source.IncludeSubFolders}" Margin="0,4">Include sub folders</CheckBox>
                        <TextBlock  DockPanel.Dock="Bottom" Text="{Binding SourceFilesStatus, FallbackValue=No files found}" Grid.Row="1"/>
                    </DockPanel>
                    <DockPanel Grid.Row="0" Grid.Column="1" LastChildFill="False">
                        <TextBlock Text="Filter" DockPanel.Dock="Top" Height="23"/>
                        <TextBox DockPanel.Dock="Top" Text="{Binding Activity.Source.Filter, UpdateSourceTrigger=PropertyChanged}" Height="23"/>
                    </DockPanel>


                </Grid>

                <DockPanel Grid.Row="2" LastChildFill="True">
                    <DockPanel DockPanel.Dock="Top" LastChildFill="False">
                        <TextBlock Text="Destinations" DockPanel.Dock="Left"/>
                        <Button Content="Add" Width="72" DockPanel.Dock="Right" Command="{Binding AddDestinationCommand}"/>
                    </DockPanel>
                    <ListBox ItemsSource="{Binding DestinationViewModelList}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <uc:DestinationView/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>

                <Border Grid.Row="3" BorderBrush="Black" BorderThickness="0,1,0,1" Margin="0,5" Padding="5">
                    <CheckBox IsChecked="{Binding Activity.DeleteSourceFiles}" Margin="0,4">Delete source files</CheckBox>
                </Border>

                <uc:ExecutionView Grid.Row="4" DataContext="{Binding ExecutionViewModel}"/>


            </Grid>

        </Grid>
    </DockPanel>
</Window>
