<UserControl x:Class="PicPick.UserControls.View.ExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PicPick.UserControls.View"
             mc:Ignorable="d" 
             d:DesignHeight="216.008" d:DesignWidth="698.949">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Content="Run" Command="{Binding StartCommand}" Height="34" Width="100" HorizontalAlignment="Left" Margin="20,0,0,0" />

        <ProgressBar Grid.Row="1" Height="26" Margin="20" Value="{Binding ProgressValue}"/>

    </Grid>
</UserControl>
