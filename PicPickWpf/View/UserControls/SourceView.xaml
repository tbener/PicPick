<UserControl x:Class="PicPick.View.UserControls.SourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PicPick.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="364.285" d:DesignWidth="509.74">
    <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,5,0" LastChildFill="False">
            <TextBlock Text="Copy from" DockPanel.Dock="Top" Height="23" Style="{StaticResource CaptionTextBlock}"/>
            <local:PathBrowserView DockPanel.Dock="Top" DataContext="{Binding PathViewModel}"/>
        </DockPanel>

        
        <!--<DockPanel Grid.Row="1" Grid.ColumnSpan="2" DockPanel.Dock="Top" LastChildFill="False" Margin="0 12 0 0">
            
        </DockPanel>-->
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0, 20">

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Files search pattern:" DockPanel.Dock="Top" Height="23"/>
                <TextBox Text="{Binding Source.Filter, UpdateSourceTrigger=PropertyChanged}" Width="240" Margin="10, 0"/>
            </StackPanel>

            <CheckBox IsChecked="{Binding Source.IncludeSubFolders}" Margin="0,4">Include sub folders</CheckBox>
            <CheckBox IsChecked="{Binding Source.OnlyNewFiles}" Margin="0,4" ToolTip="Read only files that don't exist in any of the destinations in the first place">Only new files</CheckBox>

            <StackPanel Orientation="Horizontal">
                <CheckBox Width="93" IsChecked="{Binding UseFromDate}">From date:</CheckBox>
                <DatePicker IsTodayHighlighted="True" Width="200" SelectedDate="{Binding DateFrom}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0, 5">
                <CheckBox Width="93" IsChecked="{Binding UseToDate}">To date:</CheckBox>
                <DatePicker IsTodayHighlighted="True" Width="200" SelectedDate="{Binding DateTo}"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding SourceFilesStatus, FallbackValue=Total files found: 0}" />

    </Grid>
</UserControl>
